import React from 'react';
import mediaIcon from '../../assets/icons/photoIcon.png';

const MediaButton = () => {
    return (
        <div>
            <img src={mediaIcon} alt="media"/>
        </div>
    );
};

export default MediaButton;
